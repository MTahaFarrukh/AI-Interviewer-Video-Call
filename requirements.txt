livekit-agents[google]~=1.5
livekit-api>=1.0.0
python-dotenv>=1.0.0

# Phase 2 — interview prep graph (PDF requirement: LangGraph + SQLite checkpointer)
langgraph>=0.4.0
langgraph-checkpoint-sqlite>=2.0.0

# Phase 2 — resume/JD extraction (RESEARCH.md: pdfplumber primary, pypdf fallback)
pdfplumber>=0.11.0
pypdf>=5.0.0

# Phase 7 — recruiter MCP (stdio). Not used on the LiveKit/Gemini audio path.
fastmcp>=2.0.0

# Phase 9 — PDF grading artifact
reportlab>=4.0.0

# SaaS API (Phase 1) — does not replace the LiveKit/Gemini engine
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
alembic>=1.14.0
pydantic[email]>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
pytest>=8.3.0
